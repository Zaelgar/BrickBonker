// Written by Jaidon van Herwaarden
#pragma once

#include <array>
#include <cmath>
#include <numeric>
#include <random>

namespace JMath
{
	static float Abs(float value) { return (value >= 0.0f) ? value : -value; }
}